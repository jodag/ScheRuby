(define (loop n) 
  (if (> n 0) (loop (- n 1)) 'done))
(display "loop 100")
(time (loop 100))
(display "loop 1000")
(time (loop 1000))
(display "loop 10000")
(time (loop 10000))
(display "loop 100000")
(time (loop 100000))
